import * as React from 'react';

interface OwnProps {}
interface OwnState {}

export default class Search extends React.Component<OwnProps, OwnState> {
    constructor(props) {
        super(props);
    }

    render() {
        return <span />;
    }
}
